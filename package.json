{
  "name": "goblin-core",
  "version": "0.1.0",
  "private": true,
  "license": "Apache-2.0",
  "description": "Solana-first, non-custodial planning and execution toolkit for airdrop eligibility actions.",
  "scripts": {
    "lint": "eslint .",
    "build": "tsc -b packages/core packages/solana-adapter packages/sdk packages/cli",
    "test": "vitest --run",
    "prepublishOnly": "npm run lint && npm run build && npm test"
  },
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "@eslint/js": "^8.57.0",
    "@types/node": "^20.12.7",
    "@typescript-eslint/eslint-plugin": "^7.7.0",
    "@typescript-eslint/parser": "^7.7.0",
    "eslint": "^8.57.0",
    "prettier": "^3.2.5",
    "typescript": "5.5.4",
    "typescript-eslint": "^7.7.0",
    "vitest": "^1.5.0"
  }
}
